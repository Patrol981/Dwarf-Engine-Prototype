using Voxelized.ECS;

namespace Voxelized.Loaders;

public class OBJMeshLoader : MeshLoader {
  public override Mesh Load(string path) {
    List<float> verts = new List<float>();
    int triangles = 0;
    // int 
    return new Mesh(new float[0],0);
  }
}